import { Button } from "@repo/shadcn/components/ui/button";

const page = () => {
  return (
    <div className="font-bold">
      Merchant HomePage
      <Button variant="secondary">Merchant Login</Button>
    </div>
  );
};

export default page;
